
package experimental.tiling;

public interface DistributedCollection<E, D extends DistributedCollection<E, D>> /* extends ComputationGraphNode in some way */ {
	// TODO: What about keys? When do we need them?
}
